<% content_for :title, "#{t("schedules.new.title")} — LakeDeepDiver" %>

<div class="page-head">
  <div>
    <p class="page-eyebrow"><%= t("schedules.index.eyebrow") %></p>
    <h1 class="page-title"><%= t("schedules.new.title") %></h1>
    <p class="page-sub"><%= t("schedules.new.subtitle") %></p>
  </div>
</div>

<%= render "form", schedule: @schedule %>