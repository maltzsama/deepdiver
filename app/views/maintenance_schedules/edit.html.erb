<% content_for :title, "#{t("schedules.edit.title", operation: @schedule.operation)} — LakeDeepDiver" %>

<div class="page-head">
  <div>
    <p class="page-eyebrow">
      <%= t("schedules.show.eyebrow_prefix") %>
      <%= link_to @schedule.iceberg_table.fully_qualified_name,
                  @schedule.iceberg_table, class: "link" %>
    </p>
    <h1 class="page-title"><%= t("schedules.edit.title", operation: @schedule.operation) %></h1>
  </div>
</div>

<%= render "form", schedule: @schedule %>