<% content_for :title, "#{t("catalogs.new.title")} — DeepDiver" %>

<div class="page-head">
  <div>
    <p class="page-eyebrow"><%= t("catalogs.index.eyebrow") %></p>
    <h1 class="page-title"><%= t("catalogs.new.title") %></h1>
    <p class="page-sub"><%= t("catalogs.new.subtitle") %></p>
  </div>
</div>

<%= render "form", catalog: @catalog %>