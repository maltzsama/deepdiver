{{- include "lakedeepdiver.workerDeployment" (dict
  "root" .
  "component" "worker-freshness"
  "valuesKey" "workerFreshness"
  "selector" "lakedeepdiver.workerFreshnessSelector"
  "queueConfig" "config/queue-freshness.yml"
  "queueComment" "Freshness only: isolated from long-running engine work."
  "queueEnvName" "SOLID_QUEUE_SKIP_RECURRING"
  "queueEnvValue" "true"
) -}}
