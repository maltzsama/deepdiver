{{- include "lakedeepdiver.workerDeployment" (dict
  "root" .
  "component" "worker"
  "valuesKey" "worker"
  "selector" "lakedeepdiver.workerSelector"
  "queueConfig" "config/queue-worker.yml"
  "queueEnvName" "SOLID_QUEUE_RECURRING_SCHEDULE"
  "queueEnvValue" "config/recurring.yml"
) -}}
