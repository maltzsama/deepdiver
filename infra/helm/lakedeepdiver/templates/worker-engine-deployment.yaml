{{- include "lakedeepdiver.workerDeployment" (dict
  "root" .
  "component" "worker-engine"
  "valuesKey" "workerEngine"
  "selector" "lakedeepdiver.workerEngineSelector"
  "queueConfig" "config/queue-engine.yml"
  "queueComment" "Engine lifecycle only — supervise start, drain, watchdog."
  "queueEnvName" "SOLID_QUEUE_SKIP_RECURRING"
  "queueEnvValue" "true"
) -}}
