{{- if .Values.appSecrets.create }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "deepdiver.fullname" . }}-app
  labels:
    {{- include "deepdiver.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.appSecrets.data }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
{{- end }}